/**
 * @agentix-e/timesfm-node
 *
 * Node.js ONNX Runtime inference engine for TimesFM.
 * Provides `TimesFMNodeEngine` — a production-grade `IInferenceEngine`
 * implementation backed by `onnxruntime-node`.
 *
 * @module timesfm-node
 */

export { TimesFMNodeEngine, createDefaultEngine } from './node-engine';
